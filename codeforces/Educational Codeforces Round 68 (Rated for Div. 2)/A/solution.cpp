#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int n, x, t;
    cin >> t;
    while(t--) {
        cin >> n >> x;
        cout << x*2 << '\n';
    }
    return 0;
}