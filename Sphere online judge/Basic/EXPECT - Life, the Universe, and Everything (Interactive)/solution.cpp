#include <bits/stdc++.h>
using namespace std;

int main(){
	//ios_base::sync_with_stdio(false); cin.tie(NULL);
        int n;
	while(true){
		cin >> n;
		cout << n << '\n';
		if(n==42)break;
	}
 	return 0;
}